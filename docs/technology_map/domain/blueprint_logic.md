# Blueprint: Domain Hierarchy

## Architectural Overview
Typed data models for the business domain. Defines memory-optimized, cache-aligned `structs` that encapsulate state without methods, ensuring pure functional handling.